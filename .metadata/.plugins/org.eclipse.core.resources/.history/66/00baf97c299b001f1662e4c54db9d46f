package java000_solvestringandcharacterprogramsNotes;

public class SolveStringAndCharacterProgramsNotes {

	/**
	 * Solve String Programs
	 * 
	 * Charcater Programs
	 * 
	 * Printing characters:
	 * ASCII values
	 * Only Uppercase
	 * Only Lowercase
	 * Only Digits
	 * Remove Spaces
	 * 
	 * Counting characters:
	 * Count Uppercase
	 * Lowercase
	 * Digits
	 * Vowels
	 * Any character
	 * 
	 * Creating new String:
	 * Reverse the String
	 * Check Palindrome
	 * Extract Digits
	 * Extract Alphabets
	 * Reverse Case
	 * Encoding - Piglatin
	 * 
	 * 
	 * STRING PROGRAMS:
	 * ---------------
	 * 1. Read char by char 
	 * --------------------
	 * char:
	 * -----
	 * Print:
	 * -ASCII
	 * -Uppercase
	 * -Lowercase
	 * -Digit
	 * -Remove Spaces
	 * 
	 * Count:
	 * -Uppercase
	 * -Lowercase
	 * -Digit
	 * -Vowels
	 * -Any char
	 * 
	 * New String:
	 * -Reverse
	 * -Palindrome
	 * -Only Digits
	 * -Only chars
	 * -Reverse Case
	 * -Encoding
	 * 
	 * 2. Read word by word
	 * --------------------
	 * word:
	 * ----
	 * Print:
	 * -Each Word
	 * -Starting with specific char
	 * -Word Length
	 * -Find Word
	 * -Longest Word
	 * 
	 * Count:
	 * -Count Words
	 * -Count specific words
	 * -Word Length
	 * -Starting with specific char
	 * 
	 * New String:
	 * 
	 * 
	 * 
	 * 
	 */

}
